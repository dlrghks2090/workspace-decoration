server {
    # 아래 리슨 포트는 컨테이너 기동 시 환경 변수 값으로 치환된다.
    listen       ${NGINX_PORT};
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html;
    }
}
