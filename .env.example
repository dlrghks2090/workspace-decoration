# 이 파일을 .env 로 복사한 뒤 사용한다.
#   $ cp .env.example .env
#
# .env 는 .gitignore 로 제외되며, 저장소에는 이 예시 파일만 커밋한다.

# 호스트에서 접속할 포트
HOST_PORT=8081

# 컨테이너 내부에서 nginx 가 리슨할 포트
NGINX_PORT=8080

# 애플리케이션 동작 모드
APP_MODE=production
